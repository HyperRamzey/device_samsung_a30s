# Bluetooth
bluetooth.device.default_name=Galaxy A30s

# Fingerprint
# ET715 (etspi,et7xx) on SPI1 is a rear-mounted capacitive pad; it reports its own
# DRDY interrupt, so it must not be described as an in-display optical sensor.
ro.vendor.fingerprint.type=rear
